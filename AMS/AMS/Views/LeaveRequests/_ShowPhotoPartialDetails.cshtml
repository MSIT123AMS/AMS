@model AMS.Models.LeaveRequests

@if (Model.Attachment != null)
{
    <img  src='@Url.Action("ShowPhoto","LeaveRequests",new { id=Model.LeaveRequestID})' style="width:180px;height:120px"/>
}
else
{
    <img src="@Url.Content("~/Img/upload.png")"  style="width:180px;height:120px"/>
}



@*圖片放大用,但換圖即時更改會失效
    <style>
    .zoom {
  
        transition: transform .2s; /* Animation */
        width: 200px;
        height: 200px;
        margin: 0 auto;
    }

        .zoom:hover {
            transform: scale(1.5); /* (150% zoom - Note: if the zoom is too large, it will go outside of the viewport) */
        }
</style>*@
